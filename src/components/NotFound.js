export default function NotFound() {
  return (
    <section
      style={{
        height: "50vh",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        color: "#ffc832",
      }}
    >
      <h1>** not found page</h1>
    </section>
  );
}
